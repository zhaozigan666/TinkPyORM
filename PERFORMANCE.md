# TinkPyORM 性能评估报告

**对比对象**：TinkPyORM 0.1.1 vs Python 标准库 `sqlite3`
**测试环境**：Python 3.13.14 / SQLite 3.53.1 / Windows 11 / 磁盘文件库（保留真实 fsync 开销）
**测试日期**：2026-09-03
**复现命令**：`python benchmark_vs_sqlite3.py`（约 4.7 分钟，原始数据输出至 `benchmark_results.json`）

> **数据口径**：所有数值取**三轮独立运行的中位数**。第三轮起采用**交错采样**
> （同一轮内依次跑完所有对比档位并逐轮反转顺序），以抵消系统负载漂移与磁盘缓存
> 预热造成的顺序偏差。波动率 > 30% 的项已单独标注，其倍数仅供参考。

---

## 一、核心结论

1. **ORM 的抽象开销不是性能瓶颈，事务策略才是。** 同一条 INSERT，逐条提交 `8012 µs`，单事务包裹后仅 `25.3 µs`——**相差 316 倍**。而 ORM 与手写原生代码在**相同策略下**的差距仅 **12.6 µs**（`25.3` vs `12.8`）。
2. **ORM 与"逐字直译的原生代码"性能等同。** 逐条提交场景下 ORM `8012 µs` vs 原生直译 `8061 µs`，差异 0.6%（完全持平）。ORM 没有引入额外浪费。
3. **ORM 的绝对开销在 10–50 µs 量级。** 只在"高频、单次操作、返回极少数据"时才显现；单次操作返回行越多，固定开销被摊薄得越厉害（聚合查询 `1.02×`）。
4. **真正的风险在内存，不在 CPU。** Model 实例化的内存占用是 `sqlite3.Row` 的 **4.9 倍**（87.5 MB vs 18.0 MB / 5 万行）。
5. **本次评估发现并修复了 2 个缺陷**：`insert_all` 超 5461 行崩溃；`sql_log` 无上限增长。

---

## 二、测试方法

三档对照，用于拆解开销来源：

| 档位 | 含义 | 用途 |
|---|---|---|
| **A — raw-opt** | 原生最佳实践：`executemany` / 单事务 / `sqlite3.Row` 惰性取列 | 性能上限基准 |
| **B — raw-lit** | 原生直译 ORM 语义：逐条 `execute` + 每条 `commit` + `dict` 转换 | 隔离"抽象层"与"策略"的影响 |
| **C — orm** | TinkPyORM 默认行为 | 被测对象 |

度量：`time.perf_counter`，含 warmup，三轮取中位数；每轮前置 setup（建表/清空/清日志）不计入计时；每场景独立库文件。**第三轮起改为交错采样**，避免顺序偏差——这一改动修正了首轮中 S5 原生基准被噪声拖慢导致的虚低倍数（1.12× → 真实 1.4×）。

---

## 三、全场景结果（三轮中位数）

### 3.1 写入

| 场景 | 原生最优 A | 原生直译 B | ORM 默认 C | ORM 优化写法 | ORM÷A |
|---|---|---|---|---|---|
| 逐条插入 1000（autocommit） | **12.77 µs** | 8061 µs | 8012 µs | **25.34 µs**（单事务） | 1.98× |
| 批量插入 20000 | **62.64 ms** | — | 93.62 ms（自动分批） | **85.93 ms**（事务内） | 1.37× |
| 事务内逐条插入 20000 | **73.04 ms** | — | 288.70 ms | — | 3.95× |
| 单条更新 500（单事务） | **2.94 µs** ᵃ | 76.04 µs | 98.53 µs | **13.20 µs** | 4.49× ᵃ |
| 单条删除 500（单事务） | **10.92 µs** ᵃ | 7527 µs | 7952 µs | **41.20 µs** | 3.77× ᵃ |
| 单条删除 500（autocommit） | 10.92 µs | 7527 µs | 7952 µs | — | 728× |

ᵃ 该场景 A 档波动率达 81%–128%（耗时仅 2–10 ms 的极小样本，易受调度噪声影响），倍数仅供参考；量级结论不变：ORM 优化写法与原生同处微秒级。

### 3.2 查询

| 场景 | 原生最优 A | 原生 dict B | ORM C | ORM÷A |
|---|---|---|---|---|
| 主键点查 ×20000 | **60.92 µs** | 62.33 µs | 93.05 µs | 1.53× |
| 主键点查（绕过 Builder） | 60.92 µs | — | 66.81 µs（`Db.query`） | **1.10×** |
| 条件查询 ×200（每次 ~100 行） | 82.18 µs | 99.62 µs | 126.23 µs | 1.54× ᵇ |
| 全表扫描 50000 行 | 91.53 ms | 142.12 ms | 165.30 ms | 1.81× |
| 全表扫描取单列 | 91.53 ms | — | **78.25 ms**（`column()`） | **0.85×** |
| 聚合 count ×2000 | 6032 µs | — | 6147 µs | **1.02×** |

ᵇ 独立 7 轮交错复测得 1.40×（A=76.94 ms / C=107.88 ms），区间 1.4–1.5×。

### 3.3 结果对象化与内存（20000 行查询 / 50000 行内存）

| 承载方式 | 耗时 | ORM÷A | 内存峰值（5 万行） | 内存倍数 |
|---|---|---|---|---|
| `sqlite3.Row`（原生） | 38.65 ms | 1.00× | **18.0 MB** | 1.00× |
| 原生 dict 列表 | 55.16 ms | 1.43× | 31.4 MB | 1.74× |
| ORM dict（Collection） | 60.65 ms | 1.68× | 38.1 MB | 2.12× |
| ORM Model 实例 | 119.76 ms | **3.32×** | 87.5 MB | **4.86×** |

> 内存数据在三轮中完全一致（18.0 / 31.4 / 38.1 / 87.5 MB），确定性高。

### 3.4 固定成本

| 项目 | 数值 | 说明 |
|---|---|---|
| 模块导入 | sqlite3 `9.3 ms` / TinkPyORM `28.5 ms` | 净增 19.2 ms，仅影响 CLI 冷启动 |
| SQL 构建（不执行） | f-string `0.12 µs` / Builder `24.37 µs` | 203× 差距，但绝对值仅 24 µs |

---

## 四、开销来源分解

以主键点查为例，逐层剥离 ORM 的 93.05 µs：

| 层级 | 累计耗时 | 本层增量 | 构成 |
|---|---|---|---|
| 原生 `sqlite3` + `Row` | 60.92 µs | — | SQL 执行本身 |
| + `dict` 转换 & `sql_log` | 66.81 µs | +5.9 µs | Connection 层开销 |
| + Query Builder | 93.05 µs | +26.2 µs | `where`/`limit`/`select` 解析与 SQL 拼装 |

**关键推论**：Builder 占 ORM 额外开销的 **82%**。高频点查用 `Db.query()` 绕过 Builder，可把倍数从 1.53× 压到 **1.10×**。

---

## 五、PRAGMA 调优的量化收益（ORM 逐条插入 500 条）

| 配置 | 耗时/条 | 相对默认提速 |
|---|---|---|
| 默认（journal=delete, sync=full） | 11775 µs | 1.0× |
| `synchronous=NORMAL` | 6684 µs | 1.8× |
| `synchronous=OFF` | 1403 µs | 8.4× |
| **`journal_mode=WAL` + `sync=NORMAL`** | **137.34 µs** | **85.7×** |
| `journal_mode=WAL` + `sync=OFF` | 68.70 µs | 171× |

> **WAL 是决定性的单项优化**：仅此一项即可让"逐条提交"从不可用变为可接受（11.8 ms → 0.14 ms），其收益远超任何 ORM 层面的微优化。代价是额外产生 `-wal` / `-shm` 两个文件。

---

## 六、缺陷修复记录（本次改动）

### 缺陷 1（严重，已修复）：`insert_all` 无分批保护，超 5461 行崩溃

**现象**：`sqlite3.OperationalError: too many SQL variables`
**根因**：SQLite 单语句绑定变量上限为 **32766**（实测二分确认）。`insert_all` 将全部行拼进单条 `INSERT ... VALUES (...),(...)`，6 列表单批上限为 `32766 // 6 = 5461` 行，超出即抛异常且无提示。

**修复**（`tinkpyorm/query.py`）：按 `32766 // 列数` 自动分批，分批在同一事务内执行以保证原子性；调用方已在事务中时退化为 SAVEPOINT 嵌套，语义不变。单批可容纳时走原路径，零额外开销。新增可选参数 `batch_size`。

**效果**：20000 行插入从"崩溃"变为 `93.62 ms`（原生 `executemany` 为 62.64 ms，**1.49×**）。

**验证**：7 项功能验证全部通过（超量分批、单批回归、原子性回滚、嵌套事务、fetch_sql、显式 batch_size、空列表）。

### 缺陷 2（中等，已修复）：`sql_log` 无上限增长

**现象**：`Connection.sql_log` 无条件 append，20000 次操作占用 **4.4 MB**（约 220 字节/条），长驻服务百万次请求将累积数百 MB 且永不释放。

**修复**（`tinkpyorm/connection.py` + `db.py`）：新增环形上限（默认保留最近 1000 条）与总开关。

| 配置 | 日志条数 | 内存净增 |
|---|---|---|
| 修复前（无上限） | 20000 条 | **4.4 MB** |
| 修复后（默认上限 1000） | 1000 条 | **241 KB**（−94.6%） |
| 修复后（关闭日志） | 0 条 | **18 KB**（−99.6%） |

**新 API**：`Db.sql_log_disable()` / `Db.sql_log_enable(max_size=1000)`；构造时传 `sql_log_max=None` 可恢复旧的无限行为（向后兼容）。

### 观察项（非缺陷）：默认 autocommit 语义

`Connection.execute()` 在非事务状态每次自动 `commit()`，与 ThinkPHP think-orm 行为一致（设计对齐），但在 SQLite 上代价高昂（本机约 8 ms/条）。这是**设计取舍**而非 bug，已在本报告与建议中说明正确使用姿势。

### v0.2.0 追加优化（基于本次剖析）

- **`find()` 单行快路径**：纯表查询（`model is None` 且无 `json`/`attr`/`filter`/`with`）直接
  返回 `conn.query` 首行，跳过 `_build_result` 的 `dict()` 二次拷贝与 `Collection` 包装
  （find 无结果缓存分支，返回引用安全）。行为与旧路径完全等价（旧路径对单行同样返回 dict）。
- **`journal_mode` 库内建**：`Db.set_config({"journal_mode": "WAL"})` 在连接建立后自动执行
  `PRAGMA journal_mode = WAL`（内存库跳过，非法值在配置时即抛 `ValueError`）。
  默认不设置，行为不变。
- **剖析方法**：对点查场景 10 万次迭代逐环节计时（交错采样），拆解见第五节修订说明。

---

## 七、优化建议（按优先级）

| 优先级 | 措施 | 预期收益 | 状态 |
|---|---|---|---|
| **P0** | `insert_all` 内建分批 | 消除 >5461 行崩溃 | ✅ 已修复（v0.2.0） |
| **P0** | `sql_log` 加上限/开关 | 内存降低 94.6% | ✅ 已修复（v0.2.0） |
| **P0** | 循环内写操作包裹 `Db.transaction()` | **316×** | 使用方落实（README 已给指南） |
| **P1** | `journal_mode=WAL` | 逐条提交 **85.7×** | ✅ 库内建（v0.2.0，`Db.set_config({"journal_mode": "WAL"})`） |
| **P1** | 批量写入用 `insert_all` + 显式事务 | 20000 行 85.93 ms（1.37×） | 使用方落实（README 已给指南） |
| **P2** | 高频点查用 `Db.query()` 绕过 Builder | 1.53× → **1.10×** | 使用方落实（README 已给指南） |
| **P2** | 大数据集用 `column()` 或分页 | 内存降 2–5 倍 | 使用方落实（README 已给指南） |
| **P3** | ~~Builder SQL 模板缓存~~ → 改为 **`find()` 快路径** | 见下方修订说明 | ✅ 已实施（v0.2.0） |

**P3 修订说明（实测推翻原预估）**：原建议"Builder 模板缓存省约 24 µs/次"基于点查场景
ORM 总开销 37 µs 的间接推断。本次对 10 万次迭代的逐环节剖析显示：`builder.select`
全链仅 **6.3 µs**，且"动态生成 SQL 文本 vs 常量文本"执行耗时无差异（82–84 µs，噪声内），
模板缓存理论收益上限约 5 µs/次，还需承担 SQL/参数错位风险，故放弃。点查 ~21 µs ORM
开销的真实构成是 Query 链（构造+where+limit+select，10.8 µs）与 find 方法体的 Python
调用/分配固有成本（对照主流 ORM 属正常量级）。替代实施：**`find()` 单行快路径**
（纯表查询跳过 Collection 包装与结果二次拷贝）+ **`journal_mode` 内建**（把报告中最
大量级的两个收益——事务与 WAL——从"使用方落实"下沉为一行配置）。

---

## 八、适用判断

| 场景 | 是否适用 | 依据 |
|---|---|---|
| Web API / 后台服务（QPS < 1000） | **适用** | 单请求 ORM 额外开销约 0.1–0.5 ms，占比可忽略 |
| 数据分析 / 批量 ETL | **适用，但需改造** | 用 `insert_all`（已内建分批）+ WAL；避免在 Python 层逐行处理 |
| 高频点查（>5000 QPS 单行查询） | **谨慎** | Builder 的 26 µs 是主要成本，热点路径改 `Db.query()`（1.10×） |
| 海量数据全量加载（>10 万行驻留） | **不推荐 Model 实例** | 内存 4.86×；用 `column()` 或游标分块 |
| CLI 一次性脚本 | **适用** | 导入净增 19.2 ms（v0.1.1）→ v0.4.0 为 63–78 ms，见 §10.5，仍无感 |
| 嵌入式 / 单线程低配设备 | **适用** | 无外部依赖，开销集中在 I/O 而非 CPU |

---

## 九、一句话总结

> TinkPyORM 的性能损耗在**微秒量级**，而事务与 PRAGMA 策略的影响在**毫秒量级**——相差三个数量级。
> 使用这个 ORM 时，**"怎么用"比"用不用"重要 100 倍**：把写操作放进事务、开启 WAL、批量插入走 `insert_all`，就足以让它的实际表现贴近原生，同时获得链式查询、关联预载入、软删除等生产力收益。

---

## 十、v0.4.0 追加：缓存、线程安全与流式读取的实测代价

本节记录 v0.4.0 三项改动（查询缓存重做、连接级锁、流式读取）的实际性能影响，
数据与上文同一台机器、同一方法测得。

### 10.1 连接级锁的代价：< 1%

锁只在 `Connection` 内部包一层可重入锁，单线程下每次获取为 0.1 µs 量级：

| 场景 | v0.2.0 基线 | v0.4.0（含锁） | 变化 |
|---|---|---|---|
| 事务内逐条插入（S3） | 22.2 µs/op | 22.06 µs/op | 无变化（噪声内） |
| SQL 构建（S13） | 26.7 µs/op | 26.73 µs/op | 无变化 |
| 5 万行 Model 内存（S11） | 87.5 MB | 87.5 MB | 无变化 |

**结论：锁开销相对 ORM 自身开销可忽略（< 1%）**，换来的是多线程共享连接时的事务正确性。

### 10.2 缓存收益取决于查询重量

命中缓存省去的是「SQLite 执行 + 结果物化 + 对象化」，但**保留**了 SQL 构建与缓存键计算，
因此收益随查询变重而显著上升：

| 场景 | 无缓存 | 命中缓存 | 加速比 |
|---|---|---|---|
| 内存库点查（单行、热） | 27.33 µs | 22.43 µs | 1.22× |
| 文件库点查（WAL、单行） | 37.83 µs | 29.43 µs | 1.29× |
| 文件库条件查询（约 200 行） | 671.91 µs | 132.50 µs | **5.07×** |

两点说明：

1. **点查收益有限是结构性的** —— 缓存键由 SQL 摘要生成，而 Builder 构建 SQL 占点查总开销的
   一半以上，命中路径省不掉这部分。若改为「options 摘要」作键理论上可再省 10–20 µs，但需保证
   摘要完整覆盖所有影响 SQL 的选项，风险与收益需权衡，**本期不做**。
2. **收益与查询重量成正比** —— JOIN、聚合、大结果集以及跨进程读（磁盘 IO）场景的收益远高于
   微基准中的单行点查。

### 10.3 写操作失效的成本（正确性代价）

写操作会清除同库同表的缓存，在"写一次、读一次"交替的循环中命中率趋近于 0：

```
写 + 失效 + 查询  2868.73 µs/op    ← 其中约 2000 µs 来自逐条提交的固有成本
```

这是**正确的代价**而非缺陷：数据刚变就必须重新查。使用建议：在批量写入**批次之间**读取缓存，
不要在同一次写入循环里交替读取带缓存的查询。

### 10.4 流式读取的内存效果

`chunk()` / `cursor()` 不整体物化结果集。作为对照，全量加载 5 万行的内存峰值（既有 S11 数据）为：
`sqlite3.Row` 18.0 MB / dict 列表 31.4 MB / ORM Collection 38.1 MB / Model 实例 87.5 MB；
`cursor()` 则在迭代过程中保持内存恒定（仅当前批次驻留）：

```python
# 2 万行流式读取：内存恒定
total = sum(row["n"] for row in Db.table("big").cursor(chunk_size=1000))
```

### 10.5 导入开销（已知代价）

| 版本 | import 净增 |
|---|---|
| v0.1.1（基线记录） | 19.2 ms |
| v0.4.0（本机复测，取 3 次） | 63–78 ms |

增量来自 v0.3.0 新增的 `config.py` / `drivers/` 与 v0.4.0 新增的 `cache.py`。
**仅影响 CLI 一次性脚本的冷启动**，对长驻进程（后台服务、桌面应用）无影响。
如需压缩，可考虑合并小模块或在 `Query` 中惰性导入缓存模块。

---

## 十一、v0.5.0 追加：JSON 能力实测

环境：本机 Windows 11 / Python 3.13.12 / SQLite 3.53.1，2 万行 `doc` 表
（`extra` 列存 5 字段 JSON 对象，约 120 字节），5～8 轮取中位数。

### 11.1 结果自动格式化的解码开销

| 场景 | 耗时（2 万行） | 相对基准 |
|---|---|---|
| A 不解码（`select()`） | 46.7 ms | 1.00× |
| B `json(['extra'])` 指定字段 | 126.1 ms | 2.70× |
| C `json()` 自动嗅探 | 142.1 ms | 3.04× |

**开销来源分解**：B 相对 A 净增 79.4 ms，即每行约 **4.0 µs**，
与 `json.loads` 解析一个 5 键对象的裸成本（约 3.5–4.5 µs）基本一致。
结论：**开销来自 JSON 解析本身，不是 ORM 框架层**，无框架级优化空间。

自动嗅探（C）比指定字段（B）多 16 ms（+13%），因为需对每行每个字段做一次
首尾字符判断。**生产环境推荐 `json(['字段'])` 显式声明**。

> 实践建议：不需要 JSON 对象时不要开 `json()`；只需非 JSON 列时可用
> `field()` 显式限定字段，避免把大 JSON 列读出来白白解码。

### 11.2 JSON 路径查询与表达式索引

生成 SQL 形如 `WHERE json_extract(\`extra\`, '$.city') = ?`。
建表达式索引可让 SQLite 走索引：

```sql
CREATE INDEX idx_city ON doc(json_extract(extra, '$.city'));
```

`EXPLAIN QUERY PLAN` 验证（反引号不影响表达式匹配）：

| 状态 | 执行计划 |
|---|---|
| 无索引 | `SCAN doc` |
| 有索引 | `SEARCH doc USING INDEX idx_city (<expr>=?)` |

收益高度依赖**选择性**（命中行占比）：

| 命中行数 | 无索引 | 有索引 | 提升 |
|---|---|---|---|
| 1 行（0.005%） | 13.86 ms | 0.10 ms | **134×** |
| 5000 行（25%） | 19.56 ms | 8.19 ms | 2.39× |

结论：**JSON 路径条件查询要发挥性能，必须建表达式索引**；
低选择性查询（命中比例高）的瓶颈转为结果集读取，索引收益自然收敛。

> 注意：表达式索引的表达式必须与生成 SQL 中的表达式一致
> （同一路径、同一函数），否则 SQLite 不会复用索引。

### 11.3 其它路径

| 操作 | 耗时（2 万行） | 说明 |
|---|---|---|
| `field_json()` 单路径提取 | 90.9 ms | 相对全列查询 2.03×，含 `json_extract` 计算与结果解码 |
| `where_json_contains()` 数组包含 | 13.7 ms | 基于 `json_each`，无法走索引，全表展开 |

### 11.4 写入侧

`dict` / `list` 值经 `json_encode` 编码为文本后绑定，与手工 `json.dumps` 等价，
无额外开销（2 万行批量插入 147 ms，与 v0.4.0 同量级）。

### 11.5 小结

1. JSON 解码成本 = `json.loads` 成本，**无法绕过**，只能通过"少解码"来省
   （显式字段 or 不开启）；
2. JSON 路径查询**必须配表达式索引**，高选择性场景收益达两个数量级；
3. 自动嗅探仅建议用于探索/调试，生产用显式字段声明。
